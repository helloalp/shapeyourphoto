# 用户数据目录与加密存储规范

- 用户数据统一放入被忽略的 `data/` 目录，不上传、不提交、不写入用户图片本体。
- 默认只存聚合统计，避免保存完整隐私路径。确需路径时必须说明原因并加密。
- Windows 优先使用用户级 DPAPI。DPAPI 不可用时不能把 base64 或简单编码冒充加密，应明确提示并选择保守降级。
- 旧明文数据迁移必须先读旧文件、写入新 store、验证成功后备份或标记迁移，不直接删除用户数据。
- `data/`、benchmark 报告、真实测试图片、tmp、patch、pycache 和调试输出不得进入 git。
